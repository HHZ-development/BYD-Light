{
  "compilerOptions": {
    "target": "es2020",
    "module": "commonjs",
    "lib": ["es6"], // 添加 "lib" 选项，确保ES6内置类型被识别
    "allowJs": true, // 明确允许检查JS文件
    "checkJs": true, // 开启JS文件类型检查
    "noEmit": true, // 我们只是检查，不生成任何文件，这很重要
    "strict": false, // 关闭严格模式，避免对纯JS文件进行过多不必要的检查
    "esModuleInterop": true, // 提高与CommonJS模块的兼容性
    "skipLibCheck": true, // 跳过对库声明文件的检查，能避免很多无关报错
    "forceConsistentCasingInFileNames": true,
    "resolveJsonModule": true,
    
    // 核心部分：类型定义的根目录
    "typeRoots": [
      "../.probe/client/probe-types",
      "../.probe/server/probe-types",
      "../.probe/startup/probe-types"
    ],

    // 路径别名，保持不变
    "baseUrl": ".",
    "paths": {
      "lib/*": ["./lib/*.js"]
    }
  },
  "include": [
    "./**/*.js" // 简化 include，覆盖 kubejs 文件夹下的所有JS文件
  ],
  "exclude": [
    "node_modules"
  ]
}